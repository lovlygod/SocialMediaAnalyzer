<UserControl x:Class="SocialMediaAnalyzerWPF.UserControls.PhoneSearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SocialMediaAnalyzerWPF.UserControls"
             xmlns:converters="clr-namespace:SocialMediaAnalyzerWPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Name="PhoneSearchControlRoot">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <Style x:Key="AnimatedTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource RoundedTextBoxStyle}">
            <EventSetter Event="Loaded" Handler="TextBox_Loaded" />
        </Style>
        
        <Style x:Key="HiddenScrollListViewStyle" TargetType="ListView" BasedOn="{StaticResource DarkListViewStyle}">
            <Setter Property="MaxHeight" Value="400"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListView">
                        <ScrollViewer x:Name="ScrollViewer"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      Padding="{TemplateBinding Padding}"
                                      SnapsToDevicePixels="True"
                                      HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto"
                                      Style="{StaticResource HiddenScrollViewerStyle}">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  Style="{StaticResource HiddenScrollViewerStyle}">
        <StackPanel Margin="20">
            <materialDesign:Card Style="{StaticResource RoundedCardStyle}">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="{DynamicResource PhoneSearchTitle}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource MaterialDesignBody}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                        <TextBox x:Name="PhoneTextBox"
                                 Style="{StaticResource AnimatedTextBoxStyle}"
                                 materialDesign:HintAssist.Hint="{DynamicResource EnterPhonePlaceholder}"
                                 FontSize="16"
                                 Margin="0,0,15,0"
                                 Foreground="{DynamicResource MaterialDesignBody}"
                                 Background="{DynamicResource MaterialDesignCardBackground}"
                                 KeyDown="PhoneTextBox_KeyDown"
                                 Width="300"/>
                    </StackPanel>
                    
                    <Button x:Name="SearchButton"
                            Grid.Row="1"
                            Grid.Column="1"
                            Content="{DynamicResource SearchButton}"
                            Style="{StaticResource RoundedButtonStyle}"
                            Background="{DynamicResource SecondaryAccentBrush}"
                            Foreground="White"
                            FontSize="14"
                            FontWeight="Medium"
                            Click="SearchButton_Click"/>
                </Grid>
            </materialDesign:Card>

            <materialDesign:Card Style="{StaticResource RoundedCardStyle}">
                <Expander Header="{DynamicResource SearchResultsHeader}"
                          Margin="5"
                          Style="{StaticResource DarkExpanderStyle}">
                    <Grid Margin="10">
                        <ListView x:Name="PhoneResultsListView"
                                  ItemsSource="{Binding PhoneSearchResults, ElementName=PhoneSearchControlRoot}"
                                  MaxHeight="400"
                                  Background="{DynamicResource MaterialDesignCardBackground}"
                                  Foreground="{DynamicResource MaterialDesignBody}"
                                  BorderBrush="{DynamicResource MaterialDesignDivider}"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.CanContentScroll="True"
                                  Loaded="ListView_Loaded"
                                  Style="{StaticResource DarkListViewStyle}">
                            <ListView.Template>
                                <ControlTemplate TargetType="ListView">
                                    <ScrollViewer x:Name="ScrollViewer"
                                                  Background="{TemplateBinding Background}"
                                                  BorderBrush="{TemplateBinding BorderBrush}"
                                                  BorderThickness="{TemplateBinding BorderThickness}"
                                                  Padding="{TemplateBinding Padding}"
                                                  SnapsToDevicePixels="True"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto"
                                                  Style="{StaticResource HiddenScrollViewerStyle}">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    </ScrollViewer>
                                </ControlTemplate>
                            </ListView.Template>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource MaterialDesignCardBackground}"
                                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Margin="2"
                                            Padding="8">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource PhoneNumberColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           FontWeight="Bold"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           FontWeight="Bold"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding PhoneNumber}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           FontWeight="Bold"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource FormattedNumberColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding FormattedNumber}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource LocationColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding Location}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource CarrierColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding Carrier}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource NumberTypeColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding NumberType}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource IsValidColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding IsValid}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource StatusColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding Status}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource RegionCodeColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding RegionCode}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource TimezoneColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding Timezone}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource IsPossibleColumn}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding IsPossible}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource CountryName}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding CountryName}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource Continent}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding Continent}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource AreaCode}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding AreaCode}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{DynamicResource Provider}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding Provider}"
                                                           Foreground="{DynamicResource MaterialDesignBody}"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal"
                                                       Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="{DynamicResource ErrorMessageColumn}"
                                                           Foreground="Red"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text=": "
                                                           Foreground="Red"
                                                           Margin="0,0,5,4"/>
                                                <TextBlock Text="{Binding ErrorMessage}"
                                                           Foreground="Red"
                                                           Margin="0,0,0,4"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}" />
                                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>
                </Expander>
            </materialDesign:Card>

            <materialDesign:Card Style="{StaticResource RoundedCardStyle}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ProgressBar x:Name="SearchProgressBar"
                                 Grid.Column="0"
                                 Height="6"
                                 Margin="0,10,10,10"
                                 IsIndeterminate="True"
                                 Visibility="Collapsed"
                                 Style="{StaticResource AnimatedProgressBarStyle}"/>
                    
                    <TextBlock x:Name="ProgressTextBlock"
                               Grid.Column="1"
                               Text="{DynamicResource SearchCompleted}"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource MaterialDesignBody}"/>
                </Grid>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>